# Label Auto 外接看板

Label Auto 标注平台的本地看板工具，双击即可使用，无需安装任何依赖。

## 启动方式

双击 `启动看板.bat`（或在命令行运行 `python dashboard.py`），浏览器会自动打开 `http://127.0.0.1:5221`。

> 启动器会先检测 5221 端口是否被占用，占用则自动关闭该进程后再启动。

## 登录

用 owner 账号登录（已默认预填）：

- 邮箱：`v_yuronghua@baidu.com`
- 密码：`pw123456`

> 项目数据接口要求项目成员身份，root 账号（admin@local.com）不是项目成员，会拿不到标注数据，所以默认用 owner 账号。

## 功能

| 功能 | 说明 |
|------|------|
| 标签巡检 | 选项目 + 输入标签名（如「前挡玻璃遮挡」），列出命中图、框坐标、质检状态、归属质检员，可按状态/质检员筛选，一键导出 CSV |
| 漏标告警 | 填异常标签集合，扫出「已通过(passed)但仍带这些标签」的图 |
| 质检看板 | 各项目进度、质检员通过/打回统计、标注员合格率 |
| 标签分布 | 每项目各分类框的数量条形图 |
| 导出 | 一键下载 全量/仅质检通过/YOLO 格式的标注 |
| 图片预览 | 点击结果中的图可放大查看并叠加标注框 |

## 技术说明

- 本地服务用 Python 标准库实现，零第三方依赖（Python 3 即可）。
- 服务会代理 Label Auto API（`http://172.21.205.141:8771`），解决跨域（CORS）问题。
- 导出数据有 60 秒内存缓存，反复查询不会重复拉大文件。
- 平台数据是实时的，看板每次查询都是最新快照。

## 修改平台地址

若平台地址变化，编辑 `dashboard.py` 顶部的 `BASE` 变量即可。
